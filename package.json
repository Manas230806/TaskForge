{
  "name": "taskforge",
  "version": "1.0.0",
  "description": "A powerful to-do app with reminders and focus modes",
  "main": "index.html",
  "scripts": {
    "start": "npx serve . -p 8080",
    "build": "echo 'No build step needed - static HTML'",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "cap:run:android": "npx cap run android",
    "cap:run:ios": "npx cap run ios"
  },
  "keywords": ["todo", "productivity", "pwa", "capacitor"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "serve": "^14.2.0"
  }
}
